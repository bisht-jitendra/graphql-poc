schema {
    query: Query
}

type Query {
    allUsers: [User]
    userById(id: Long): User
    userByIdIn(ids: [Long]): [User]
}

type User {
    id: Long
    name: String
    age: String
    address: String
    createdAt: String
}