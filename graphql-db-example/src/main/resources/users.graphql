schema {
    query: Query
}

type Query {
    allUsers(skip: Int = 0, first: Int = 0): [User]
    userById(id: Long): User
    userByIdIn(ids: [Long]): [User]
}

type User {
    id: Long
    name: String
    age: Int
    address: String
    createdAt: String
}